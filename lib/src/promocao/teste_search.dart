
import 'package:flutter/cupertino.dart';

class TesteSearch extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("search"),
      ),
    );
  }
}
